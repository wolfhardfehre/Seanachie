<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/recycler_item_image_story_card"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="@dimen/card_margin"
    app:cardCornerRadius="@dimen/card_corners"
    app:cardElevation="@dimen/card_elevation">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/recycler_item_image_story_image"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_gravity="center_vertical"
            android:layout_weight="0.5"
            android:scaleType="centerCrop"
            android:src="@drawable/seanachie"
            tools:ignore="ContentDescription" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_weight="0.2"
            android:paddingStart="@dimen/standard_padding"
            android:paddingEnd="@dimen/standard_padding"
            android:padding="@dimen/text_outer_padding"
            android:orientation="vertical" >

            <TextView
                android:id="@+id/recycler_item_image_story_name"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:paddingBottom="@dimen/text_divider_padding"
                android:text="@string/recycler_forms_heading"
                android:textColor="@color/primary_text"
                android:textSize="@dimen/text_medium" />

            <TextView
                android:id="@+id/recycler_item_image_story_first_category"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@string/recycler_forms_content"
                android:textColor="@color/secondary_text"
                android:textSize="@dimen/text_hint_size" />

        </LinearLayout>
    </LinearLayout>
</android.support.v7.widget.CardView>